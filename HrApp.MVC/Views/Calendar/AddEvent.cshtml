@using HrApp.Application.Calendars.Command.CreateCalendarEvent
@model CreateCalendarEventCommand

@{
    ViewData["Title"] = "Dodaj wydarzenie do kalendarza";
}

<h2>@ViewData["Title"]</h2>

@if (!(bool)ViewBag.IsConnected)
{
    <div class="alert alert-warning">
        <p>Nie jesteś połączony z Google Kalendarzem.</p>
        <a href="/calendar/connect" class="btn btn-primary">Połącz z Google</a>
    </div>
}
else
{
    <form asp-action="AddEvent" method="post">
        <div class="form-group">
            <label asp-for="Title"></label>
            <input asp-for="Title" class="form-control" />
            <span asp-validation-for="Title" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="StartDate">Data rozpoczęcia</label>
            <input asp-for="StartDate" type="datetime-local" class="form-control" />
            <span asp-validation-for="StartDate" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="EndDate">Data zakończenia</label>
            <input asp-for="EndDate" type="datetime-local" class="form-control" />
            <span asp-validation-for="EndDate" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="Description">Opis</label>
            <textarea asp-for="Description" class="form-control"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary">Dodaj wydarzenie</button>
    </form>
}
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
